# Backend API adresi. Varsayılan: http://localhost:3000
# Bu dosyayı .env.local olarak kopyala: cp .env.example .env.local
NEXT_PUBLIC_API_URL=http://localhost:3000

# Sitenin kendi adresi — sitemap, robots ve paylaşım önizlemeleri için.
# Vercel'de gerekmiyor (VERCEL_PROJECT_PRODUCTION_URL kullanılıyor).
NEXT_PUBLIC_SITE_URL=http://localhost:3001
